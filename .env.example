# ============================================
# RAG Tutor Web - 环境变量配置示例
# ============================================
# 使用说明：
# 1. 复制此文件为 .env
# 2. 根据你的实际情况填写配置项
# 3. 注意：.env 文件包含敏感信息，已被 .gitignore 忽略，不会提交到 Git
# ============================================

# ========== 服务器运行配置 ==========
# 环境类型：dev (开发) / production (生产)
APP_ENV=production

# 服务器监听地址和端口
APP_HOST=0.0.0.0
APP_PORT=8000

# 应用基础URL
BASE_URL=http://localhost

# ========== JWT 配置 ==========
# JWT 密钥（用于 token 签名，请务必修改为强随机字符串！）
# 生成方法：openssl rand -hex 32
JWT_SECRET=PLEASE_CHANGE_THIS_TO_A_SECURE_RANDOM_STRING

# JWT 过期时间（分钟），10080 = 7天
JWT_EXPIRE_MINUTES=10080

# ========== 千问 (Qwen) 模型配置 ==========
# API KEY 获取地址: https://dashscope.console.aliyun.com/
# 注意：这是必填项，需要从阿里云获取
QWEN_API_KEY=your_dashscope_api_key_here

# 使用的千文模型名称
QWEN_MODEL=qwen-plus

# ========== OpenAI 配置（可选） ==========
# 如果使用 OpenAI 兼容接口，需要配置以下内容
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1

# ========== 数据库配置 ==========
# SQLite 数据库路径（Docker 容器内使用绝对路径）
DATABASE_URL=sqlite:////app/data/app.db

# ========== 数据与索引目录 ==========
# Docker 容器内使用绝对路径 /app/data
# 容器挂载点：backend_data:/app/data
DATA_DIR=/app/data
PDF_DIR=/app/data/pdfs
INDEX_DIR=/app/data/index

# ========== RAG 参数配置 ==========
# 检索返回的文档数量
TOP_K=6

# 文档分块大小（字符数）
CHUNK_SIZE=700

# 文档分块重叠大小（字符数）
CHUNK_OVERLAP=120
