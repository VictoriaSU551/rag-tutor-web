# 移动端适配快速指南

## 📱 核心特性一览

### 响应式断点

```
手机: ≤ 768px  → 单列竖屏
平板: 769-1024px → 可调多列
桌面: > 1024px  → 三列固定
```

### 关键改进清单

#### ✅ 视口设置（Viewport）
- 正确的缩放控制
- iOS 安全区域适配（iPhone 12+ 刘海屏）
- PWA 应用清单准备

#### ✅ 响应式布局
- **手机** → 对话区全屏（隐藏会话列表和练习题）
- **平板** → 自适应三列
- **桌面** → 可拖拽三列（原有功能保留）

#### ✅ UI 组件适配
| 组件 | 手机 | 平板 | 桌面 |
|------|------|------|------|
| 按钮大小 | 44×44px | 44×48px | 48×48px |
| 文字大小 | 12-14px | 13-16px | 14-18px |
| 间距 | 8px | 12px | 16px |
| 输入框高度 | 40px | 40px | 48px |

#### ✅ 模态框
- **登录框** → 居中或全屏
- **错题本** → 底部弹出（手机）或居中（桌面）
- **知识库** → 底部弹出（手机）或居中（桌面）

#### ✅ 触摸优化
- 移除蓝色点击高亮
- 所有交互元素 ≥ 44px
- 防止 iOS 自动缩放

---

## 🎨 使用的 Tailwind 类

### 响应式前缀
```html
<!-- 仅在小屏幕显示 -->
<div class="hidden md:block">仅桌面显示</div>

<!-- 响应式文字 -->
<h1 class="text-lg md:text-2xl">标题</h1>

<!-- 响应式间距 -->
<div class="px-3 md:px-4 py-2 md:py-3">内容</div>

<!-- 响应式布局 -->
<div class="flex flex-col md:flex-row">内容</div>
```

### 常用断点
- `sm:` → ≥ 640px
- `md:` → ≥ 768px
- `lg:` → ≥ 1024px

---

## 🔍 检查清单

### 浏览器测试
- [ ] Chrome 移动版
- [ ] Safari iOS
- [ ] Firefox 安卓
- [ ] 系统浏览器

### 功能测试
- [ ] 输入和发送消息
- [ ] 显示长消息（自动换行）
- [ ] 显示代码块（水平滚动）
- [ ] 显示表格（水平滚动）
- [ ] 打开模态框（是否遮挡）
- [ ] 上传 PDF
- [ ] 删除 PDF

### 设备测试
- [ ] 小屏手机（≤ 375px）
- [ ] 中屏手机（375-480px）
- [ ] 大屏手机（480-768px）
- [ ] 平板竖屏（768-900px）
- [ ] 平板横屏（> 900px）

### 特殊场景
- [ ] iPhone with Notch（刘海屏）
- [ ] iPhone with Dynamic Island（灵动岛）
- [ ] 手动缩放页面（Pinch Zoom）
- [ ] 横屏旋转
- [ ] 软键盘弹出

---

## 📝 CSS 媒体查询

### 手机适配（≤ 768px）
```css
@media (max-width: 768px) {
  /* 垂直堆叠布局 */
  .layout-container { flex-direction: column; }
  
  /* 隐藏非必要面板 */
  [data-section="sessions"],
  [data-section="quiz"] { display: none; }
  
  /* 更大的点击区域 */
  button, input, textarea { min-height: 44px; }
  
  /* 防止 iOS 缩放 */
  input, textarea { font-size: 16px; }
}
```

### 超小屏幕（< 640px）
```css
@media (max-width: 640px) {
  /* 进一步减小文字 */
  .message-content { font-size: 12px; }
  
  /* 减小间距 */
  button { padding: 10px 12px; }
}
```

### 平板（769-1024px）
```css
@media (min-width: 769px) and (max-width: 1024px) {
  /* 调整列宽 */
  [data-section="sessions"] { flex-basis: 180px; }
  [data-section="quiz"] { flex-basis: 200px; }
}
```

---

## 🚀 部署提示

### 构建配置
```bash
# 无需额外配置，HTML + CSS 已优化
# 直接部署即可
```

### 服务器头（推荐）
```
Cache-Control: public, max-age=3600
Content-Type: text/html; charset=utf-8
X-UA-Compatible: IE=edge
```

### PWA 准备（可选）
```html
<!-- 添加到 <head> -->
<link rel="manifest" href="/manifest.json">
<link rel="icon" type="image/png" href="/icon.png">
<meta name="theme-color" content="#ff8c00">
```

---

## 🐛 常见问题

### Q: 手机上输入框被软键盘遮挡？
A: 已设置 `min-height: 44px`，浏览器会自动处理。如问题持续，检查是否有绝对定位元素。

### Q: iOS 上输入框自动缩放？
A: 已设置 `font-size: 16px` 防止缩放。

### Q: 模态框在手机上显示不全？
A: 设置了 `max-h-[90vh] overflow-y-auto`，内容可滚动。

### Q: 平板上如何显示侧边栏？
A: > 768px 时自动显示，可通过 `md:` 类控制。

### Q: 如何禁用缩放？
A: 在 meta viewport 中改为 `maximum-scale=1` 和 `user-scalable=no`（不推荐，影响可访问性）。

---

## 📚 参考资源

- [MDN: Viewport Meta Tag](https://developer.mozilla.org/en-US/docs/Web/HTML/Viewport_meta_tag)
- [Apple: Safe Area](https://webkit.org/blog/7929/designing-websites-for-iphone-x/)
- [Tailwind: Responsive Design](https://tailwindcss.com/docs/responsive-design)
- [Web.dev: Mobile Friendly](https://web.dev/responsive-web-design-basics/)

---

**更新日期**: 2026年2月3日  
**版本**: 3.0 Mobile-Ready
